// import React from 'react';
// import { shallow } from 'enzyme';
// import Loading from './index'; // 引入对应的 React 组件

// it('renders with Result', () => {
//   const wrapper = shallow(<Loading />); // 进行渲染
//   expect(wrapper.find('Loading').length).toBe(1); // 有 Result 组件
//   // expect(wrapper.find('Loading').prop('type')).toBe('success');    // Result 组件的类型是成功
// });
